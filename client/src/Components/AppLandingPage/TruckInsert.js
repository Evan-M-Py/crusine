import React, { useState } from "react";
import { Button, Row, Col, Container } from 'react-bootstrap';

const  TruckInsert = (props) => {
    const [ text, setText ] = useState({
        truck_name: ''
    })
};